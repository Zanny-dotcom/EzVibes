# ezvibes Launcher and Start Menu Shortcut

## Goal
Make Claude Control Panel launchable like a normal Windows application: searchable from the Start menu as "ezvibes", pinnable to the taskbar, optionally auto-started at sign-in, and visually represented by a classic yellow folder icon with a black letter Z. Launching it must not flash a console window. This mirrors the existing pattern in `C:\Users\Oskari\Documents\terminal-emulator` (Retteli Terminaali).

## Current State
- CP is started today by running `npm start` in a terminal, which invokes `electron .` and inherits that terminal's window.
- There is no Start Menu entry, taskbar pin, app icon, or .ico file for CP.
- `renderer/` contains only `app.js`, `index.html`, `styles.css` — no `favicon.ico` or `favicon.png`.
- `package.json` has no `productName` icon wiring and no shortcut/build scripts. Electron uses its default icon on the running window and in the taskbar.
- `scripts/` contains only `postinstall.js` (rebuilds `node-pty`).
- The `startup/` directory does not exist.
- The reference implementation at `C:\Users\Oskari\Documents\terminal-emulator` ships:
  - `Retteli Terminaali.vbs` at the project root (silent Electron launcher).
  - `renderer/favicon.ico` (multi-resolution icon).
  - `startup/Install-RetteliShortcuts.ps1` (creates Start Menu shortcut, refreshes taskbar pin, optional Startup folder shortcut).

## Target State
- A user can press Win, type `ezvibes`, hit Enter, and CP opens with no console window flash.
- A user can right-click the Start menu result and choose "Pin to taskbar"; after re-running the installer, that taskbar pin uses the Z folder icon and points at `wscript.exe ezvibes.vbs`.
- A user can opt into auto-launch at sign-in by running the installer with `-CreateStartupShortcut`.
- The shortcut's label is exactly `ezvibes` (lowercase). The icon is a classic warm-yellow manila folder with a bold black sans-serif "Z" centered on the folder face.
- The icon is generated by a committed PowerShell script using .NET `System.Drawing`. The generator emits a multi-resolution `.ico` (16/24/32/48/64/128/256 px) and writes it to `renderer/ezvibes.ico`. Both the generator and the resulting `.ico` are checked into the repo so install does not depend on .NET drawing succeeding.
- All new behavior is opt-in via running the installer; nothing in the normal `npm start` developer flow changes.

## Out of Scope
- Replacing the Electron BrowserWindow icon at runtime (the running window's titlebar/taskbar icon comes from the shortcut's target/icon binding — covered by this design — and from Electron's BrowserWindow `icon` option, which is unrelated to this spec). If we later want the in-app window icon to match, that is a separate small change to `main.js` (`new BrowserWindow({ icon: ... })`) and is not part of this work.
- A packaged installer (.msi, .exe) or a fully-bundled distributable. We're shipping a launcher + shortcut, not an installer artifact.
- Multi-user / system-wide installation. Shortcuts are written under `%APPDATA%`, i.e. per-user.
- Removing or unpinning previously-created shortcuts (no uninstall script). The user can delete `.lnk` files manually if needed.
- Creating a new taskbar pin from scratch (Windows blocks this for security on modern versions). The script only *refreshes* an existing pin; the user must pin once via right-click from the Start menu result.
- Changing the app's display name in `package.json` (`productName` stays `"Claude Control Panel"`). The shortcut's name is independent.

## Components

### 1. Launcher — `ezvibes.vbs` (project root)
- VBScript run via `wscript.exe` (not `cscript.exe`) so there is no console window.
- Resolves the app directory from `WScript.ScriptFullName` so the launcher is path-portable (works even if `CP\` is moved or copied).
- Verifies `node_modules\electron\dist\electron.exe` exists; if missing, shows a `MsgBox` telling the user to run `npm install` in the app directory and exits with code 1.
- Sets `shell.CurrentDirectory` to the app dir, then calls `shell.Run "<electron.exe> ."` with `intWindowStyle=1` (normal window) and `bWaitOnReturn=False` (fire-and-forget).
- File contents:
  ```vbs
  Option Explicit

  Dim shell, fso, appDir, electronExe

  Set shell = CreateObject("WScript.Shell")
  Set fso = CreateObject("Scripting.FileSystemObject")

  appDir = fso.GetParentFolderName(WScript.ScriptFullName)
  electronExe = appDir & "\node_modules\electron\dist\electron.exe"

  If Not fso.FileExists(electronExe) Then
    MsgBox "Electron is not installed. Run npm install in " & appDir & " first.", vbExclamation, "ezvibes"
    WScript.Quit 1
  End If

  shell.CurrentDirectory = appDir
  shell.Run """" & electronExe & """ .", 1, False
  ```

### 2. Icon generator — `scripts/build-ezvibes-icon.ps1`
- PowerShell script, ~100–150 lines. Uses only types from `System.Drawing` and `System.Drawing.Imaging` (loaded via `Add-Type -AssemblyName System.Drawing`).
- For each target size in `16, 24, 32, 48, 64, 128, 256`:
  1. Create a transparent 32-bpp ARGB `Bitmap` of that size.
  2. Get a `Graphics`, set `SmoothingMode = AntiAlias`, `InterpolationMode = HighQualityBicubic`, `TextRenderingHint = AntiAliasGridFit`.
  3. Draw the folder back tab (small rectangle at the top-left of the folder body).
  4. Draw the folder front face as a rounded rectangle with a vertical `LinearGradientBrush` from warm yellow `#F5C24A` (top) to deeper yellow `#E0A828` (bottom).
  5. Stroke both shapes with a thin darker yellow outline `#8A6020` (single-pixel at small sizes, 1.5–2 px at large sizes).
  6. Compose a black "Z" using `Segoe UI Black` (fall back to `Arial Black`, then `Impact`) sized to ~55% of the folder face height. Center horizontally and vertically over the front face. Use `StringFormat` with `Alignment = Center` and `LineAlignment = Center`.
  7. For 16×16 and 24×24, flatten the gradient (use a solid `#EDB840`) and skip the rounded-corner radius (use a plain rectangle) to avoid muddy pixels.
  8. Encode the bitmap to a PNG byte array via `Bitmap.Save(MemoryStream, ImageFormat.Png)`.
- Pack the PNG byte arrays into a valid ICONDIR / ICONDIRENTRY structure (PNG-in-ICO; supported by Vista+):
  - 6-byte `ICONDIR` header: `Reserved=0`, `Type=1`, `Count=N`.
  - For each image: 16-byte `ICONDIRENTRY` (`Width`, `Height` — both stored as `0` for 256, otherwise the size; `ColorCount=0`; `Reserved=0`; `Planes=1`; `BitCount=32`; `BytesInRes`; `ImageOffset`).
  - Then the concatenated PNG payloads.
  - Write the whole blob to `renderer/ezvibes.ico` via `[System.IO.File]::WriteAllBytes(...)`.
- Idempotent: overwrites the existing `.ico` on each run.
- Prints the absolute output path on success.

### 3. Shortcut installer — `startup/Install-EzvibesShortcuts.ps1`
- PowerShell script adapted from `terminal-emulator/startup/Install-RetteliShortcuts.ps1`, retargeted at CP.
- Param: `[switch]$CreateStartupShortcut`.
- Sets `$ErrorActionPreference = 'Stop'`.
- Resolves:
  - `$repoRoot = Resolve-Path -LiteralPath (Join-Path $PSScriptRoot '..')`
  - `$launcherPath = Join-Path $repoRoot 'ezvibes.vbs'`
  - `$iconPath = Join-Path $repoRoot 'renderer\ezvibes.ico'`
- Throws with a clear message if either path is missing.
- Defines `function New-EzvibesShortcut { param([string]$ShortcutPath) ... }` that:
  - Creates the parent directory with `New-Item -ItemType Directory -Force` (so Startup folder etc. is created if missing).
  - Builds the `.lnk` via `New-Object -ComObject WScript.Shell` and sets:
    - `TargetPath` = `Join-Path $env:WINDIR 'System32\wscript.exe'`
    - `Arguments` = `'"' + $launcherPath + '"'`
    - `WorkingDirectory` = `[string]$repoRoot`
    - `IconLocation` = `$iconPath + ',0'`
    - `Description` = `'Launch ezvibes'`
  - Calls `$shortcut.Save()`.
- Always-run steps:
  - **Start Menu**: write `%APPDATA%\Microsoft\Windows\Start Menu\Programs\ezvibes.lnk`. This is the entry Windows Search indexes.
  - **Taskbar pin refresh**: check `%APPDATA%\Microsoft\Internet Explorer\Quick Launch\User Pinned\TaskBar\`. If a stale `Electron.lnk` exists *and* `ezvibes.lnk` does not, rename it. Then, if `ezvibes.lnk` exists, rewrite it via `New-EzvibesShortcut`. This keeps a previously-created pin in sync with the latest launcher/icon. If no pin exists, the script prints a hint telling the user to pin manually from Search.
- Opt-in step (if `-CreateStartupShortcut`):
  - Write `%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup\ezvibes.lnk` via the same helper.
- Refresh Explorer icon cache: invoke `Start-Process -FilePath $ie4uinit -ArgumentList '-show' -WindowStyle Hidden -ErrorAction SilentlyContinue` where `$ie4uinit = Join-Path $env:WINDIR 'System32\ie4uinit.exe'`. Best-effort; skip silently if missing.
- Print a summary listing every shortcut path that was created or refreshed, and the manual-pin hint if applicable.

### 4. Installer README — `startup/README.md`
- Short markdown file with three usage examples:
  1. Basic install (Start Menu + refresh taskbar pin if present).
  2. Install with `-CreateStartupShortcut` (also adds Startup folder shortcut).
  3. One-time icon generation: `./scripts/build-ezvibes-icon.ps1`.
- Notes that the first-time taskbar pin must be done manually from the Start menu search result.

### 5. CLAUDE.md update
- Add a subsection under **Launch** (or a new top-level **ezvibes Launcher** section) summarizing:
  - The three new files (`ezvibes.vbs`, `renderer/ezvibes.ico`, `startup/Install-EzvibesShortcuts.ps1`) plus the generator (`scripts/build-ezvibes-icon.ps1`).
  - The install command and how to search/pin.
  - The manual-pin requirement.
- Add a bullet to **Known Limitations / Next Improvements**:
  - "First-time taskbar pin still requires the user to right-click the Start menu result and select 'Pin to taskbar'; subsequent re-runs of the installer keep the pin in sync."

## Data Flow

```
User presses Win, types "ezvibes", hits Enter
  → Windows Search resolves to %APPDATA%\Microsoft\Windows\Start Menu\Programs\ezvibes.lnk
  → Shell launches the .lnk:
      target    = C:\Windows\System32\wscript.exe
      arguments = "C:\Users\Oskari\Documents\CP\ezvibes.vbs"
      workdir   = C:\Users\Oskari\Documents\CP
      icon      = C:\Users\Oskari\Documents\CP\renderer\ezvibes.ico,0
  → wscript.exe runs ezvibes.vbs (no console window)
  → VBS resolves appDir, verifies electron.exe exists
  → VBS sets CurrentDirectory and calls shell.Run "<electron.exe> ."
  → Electron starts CP normally; the taskbar shows the .lnk's icon
```

Installation flow:

```
./scripts/build-ezvibes-icon.ps1
  → renders 7 bitmaps, packs ICO, writes renderer/ezvibes.ico
./startup/Install-EzvibesShortcuts.ps1 [-CreateStartupShortcut]
  → validates launcher + icon exist
  → writes Start Menu shortcut
  → optionally renames stale Electron.lnk and refreshes taskbar pin
  → optionally writes Startup folder shortcut
  → invokes ie4uinit.exe -show (icon cache refresh)
  → prints summary
```

## Error Handling
- Missing `ezvibes.vbs` or `renderer/ezvibes.ico` at install time → installer throws with the missing path. The user fixes by running `npm install` (for Electron) and `./scripts/build-ezvibes-icon.ps1` (for the icon).
- Missing `electron.exe` at launch time → VBS shows a `MsgBox` instructing the user to run `npm install`, then exits with code 1. No silent failure.
- `ie4uinit.exe` missing or fails → install script continues; the new icon may take a logoff/login cycle to appear, but the shortcut is still functional.
- Pre-existing `Electron.lnk` in the taskbar pin folder *and* pre-existing `ezvibes.lnk` → script does not move the stale one (assumes `ezvibes.lnk` is the live pin), and refreshes only `ezvibes.lnk`. The stale `Electron.lnk` is left alone for the user to remove if desired.
- Font fallback chain in the icon generator: if `Segoe UI Black` is unavailable, try `Arial Black`, then `Impact`, then the default sans-serif. Drawing never fails because of font availability.
- ICO packing: every offset and `BytesInRes` is computed from actual payload lengths, so a partial / corrupt write would only occur on disk I/O failure (which `WriteAllBytes` surfaces as an exception that propagates to the user).

## Risk / Notes
- **Taskbar pin creation is not scriptable on modern Windows.** Pin refresh works only after the user pins once via right-click on the Start menu result. This is a Windows limitation, not a design choice; documented in CLAUDE.md and the installer's printed summary.
- **Per-user only.** Shortcuts under `%APPDATA%` mean each Windows user has to run the installer themselves. This matches Retteli's behavior and is the right scope for a single-developer tool.
- **`.ico` is checked into the repo.** This is intentional: installs don't need to regenerate, and the generator script + icon together act as source + artifact for easy review of any visual change.
- **Repo-path coupling.** The shortcut bakes in the absolute path to `ezvibes.vbs`. If the CP folder is moved, the user must re-run the installer. The VBS itself remains path-portable (resolves its own location), so only the shortcuts need refreshing.
- **No `package.json` changes.** This keeps the dev `npm start` workflow untouched and avoids muddying the existing scripts.
- **PNG-in-ICO format support.** Targets Vista+, which is fine for any system running modern Electron. The generator does *not* fall back to BMP-in-ICO for older systems because none are in scope.
- **Window icon vs shortcut icon.** The shortcut's icon binding controls what shows in the taskbar for the running app *only because Electron's default AppUserModelID gets associated with the shortcut on launch*. If we later notice the icon reverts (e.g., when launched via a different code path), the fix is to set `app.setAppUserModelId('ezvibes')` in `main.js` and rebuild the shortcut — out of scope here but worth recording.
- **Reference implementation parity.** Behavior tracks Retteli's installer one-for-one (Start Menu + pin refresh + optional Startup + icon cache refresh). Anything Retteli supports, ezvibes supports the same way; anything Retteli does not support (uninstall, MSI, per-machine install), neither does ezvibes.
