{
  "name": "claude-control-panel",
  "productName": "Claude Control Panel",
  "version": "0.1.0",
  "description": "Explorer-style control panel for launching folder-scoped Claude terminal sessions.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "postinstall": "node scripts/postinstall.js",
    "rebuild": "npx @electron/rebuild"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0",
    "node-pty": "1.1.0"
  },
  "devDependencies": {
    "@electron/rebuild": "^4.0.0",
    "electron": "^33.0.0"
  }
}
